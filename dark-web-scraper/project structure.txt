darkweb_scraper/
│── app/
│   │── __init__.py
│   │── main.py                 # FastAPI entrypoint
│   │── config.py               # Configuration (Tor, Redis, DB, etc.)
│   │── models.py               # Pydantic models for API request/response
│   │── routes/
│   │   │── __init__.py
│   │   │── search.py           # Targeted search endpoints
│   │   │── monitor.py          # Keyword monitoring endpoints
│   │   │── results.py          # Fetch results by task_id
│   │── tasks/
│   │   │── __init__.py
│   │   │── search_tasks.py     # Celery tasks for targeted search
│   │   │── monitor_tasks.py    # Celery tasks for continuous monitoring
│   │── scraping/
│   │   │── __init__.py
│   │   │── base_scraper.py     # Common scraping logic
│   │   │── forum_scraper.py    # Forum-specific scraping logic
│   │   │── marketplace_scraper.py # Marketplace-specific scraping
│   │   │── paste_scraper.py    # Paste site scraping
│   │── utils/
│   │   │── __init__.py
│   │   │── tor_client.py       # Tor SOCKS5 proxy + stem integration
│   │   │── parser.py           # HTML parsing helpers (BeautifulSoup)
│   │   │── alerts.py           # Alerting system (send hits back to main app/logging)
│   │   │── logger.py           # Custom secure logging (no PII)
│
│── worker.py                   # Celery worker bootstrap
│── requirements.txt            # Python dependencies
│── Dockerfile                  # Container setup
│── docker-compose.yml          # Redis + service orchestration
│── README.md                   # Documentation
